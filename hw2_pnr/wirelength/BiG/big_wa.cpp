#include "big_wa.h"
#include <numeric>
#include <vector>

double BigWA::forwardAndBackward(const std::vector<double> &pos, double gamma,
                                 std::vector<double> &grad) {
    /* TODO: complement the forward and backward calculation */
}
